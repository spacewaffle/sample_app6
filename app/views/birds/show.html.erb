<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @bird.name %>
</p>

<p>
  <strong>Type:</strong>
  <%= @bird.bird_type %>
</p>

<% @bird.tweets.each do |tweet| %>
  <p>
    <%= tweet.content %>
  </p>
<% end %>

<%= link_to 'Edit', edit_bird_path(@bird) %> |
<%= link_to 'Back', birds_path %>
